# 牙齿与嘴部表现优化版本记录

## 版本规则

- 大版本：`v1`、`v2`，用于主要优化路线或模型方案变化。
- 小版本：`v1.1`、`v1.2`，用于同一路线下有明确记录的参数或实现调整。
- 提交及标签备注：`版本 - 优化位置；使用方法；实际效果与局限`。
- 换参考图片、追加案例不自动算算法优化；无效实验可以保留代码，但不冒称新优化版本。
- 私人素材、生成视频、服务器信息、完整日志及模型权重不进入此代码存档。

## v1 - 嘴部初始状态调整：采用原版嘴唇归一化

### 优化了哪里

针对源图本身带有露齿笑容，叠加驱动动作后持续或过度露齿的现象，调整动画起始嘴部状态。目标不是通过减少全局动作幅度或模糊图像掩盖问题。

### 使用什么方法

- 启用原版 `flag_normalize_lip`，保留 `driving_multiplier=1.0`。
- 保留相对运动和stitching；不启用眼/嘴重定向，不启用torch.compile。
- 使用原版F/M/W/G及控制模块权重；核心推理源码与 `9b294b3d0536135442ea73cb01e6cb3ca7029dd3` 一致。
- 参数快照见 `presets/v1.json`。这是记录文件，原版CLI不会自动读取它。
- 为避免音轨合并截短尾帧，运行流程先使用独立无音轨副本，再将原音轨以stream copy合回，省略`-shortest`，并核验帧数及音轨一致性。**这是一项外部执行流程，不是已经改好了上游导出函数。**

### 效果如何

- 已完成同一照片、同一驱动的完整视频前后对照，用户反馈当前版本可以接受。
- 采样观察：部分闭嘴时刻不再保持原版的大幅露齿，整体露齿幅度更克制。
- 已验证完整输出帧数及原音轨保留。
- 局限：部分轻微张口可能被压成接近闭嘴；没有证明牙缝清晰度、所有口型、所有身份或所有视频都改善。
- 没有微调训练，也没有“优化后权重”；这是用户认可的参数工作流版本。

### 复现说明（只在已获准的GPU环境执行）

先在新的案例目录中对驱动视频做video-only stream copy，不修改原文件：

```text
ffmpeg -n -i <driving.mp4> -map 0:v:0 -c:v copy -an <driving_silent.mp4>
python inference.py -s <source.jpg> -d <driving_silent.mp4> -o <new-output-directory> --flag-normalize-lip --driving-multiplier 1.0 --source-max-dim 1280
```

原驱动有音轨时，合并到新的输出文件，不覆盖无音轨结果：

```text
ffmpeg -n -i <generated-silent.mp4> -i <original-driving.mp4> -map 0:v:0 -map 1:a:0 -c:v copy -c:a copy <generated-with-audio.mp4>
```

音频stream copy需要容器兼容；当前已验证AAC/MP4，不保证任意音频编码。必须检查实际帧数、音轨起点及长度。未含音轨时跳过合并。

上游默认CRF实际为18，人脸原生生成512×512。不要把整幅输出放大当作恢复真实牙齿细节。

### 未采用的候选

- 仅把F/W/G改成FP32：同人物小样例未见明确收益。
- 统一使用露齿源图：局部改善但其他口型可退化。
- 原权重直接将外观输入改为512：非训练尺度下出现嘴形/眼睑回归，不采用。
